<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.incongress.xhy_guke">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".base.XhyApplication"
        android:theme="@style/AppBaseTheme_Light">

        <activity android:name="cn.incongress.xhy_guke.activitys.HomeActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activitys.VVTalkDetailActivity"
            android:windowSoftInputMode="stateHidden|adjustPan"
            android:parentActivityName=".activitys.HomeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activitys.HomeActivity" />
        </activity>
    </application>

</manifest>
